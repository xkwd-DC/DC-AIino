# 复制为 .env 后按需修改。.env 已在 .gitignore 中。

# Flask 监听端口（dev: python app.py 使用；prod: gunicorn -b 自己指定）
PORT=5000

# 允许跨域的前端来源。demo 阶段 *，生产收窄到具体域名，多个用逗号分隔。
# 例：CORS_ORIGINS=https://grain-risk.example.com,http://localhost:5173
CORS_ORIGINS=*
